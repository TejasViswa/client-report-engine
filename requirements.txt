# Core dependencies
docxtpl>=0.16.7
python-docx>=1.1.0
Jinja2>=3.1.2

# PDF conversion (optional - can also use LibreOffice)
docx2pdf>=0.1.8; sys_platform == "win32" or sys_platform == "darwin"

# Development & testing
pytest>=7.4.0
pytest-cov>=4.1.0

